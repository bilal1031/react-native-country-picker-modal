import * as React from 'react';
import { ModalProps } from 'react-native';
export declare const CountryModal: ({ children, withModal, disableNativeModal, ...props }: ModalProps & {
    children: React.ReactNode;
    withModal?: boolean;
    disableNativeModal?: boolean;
}) => React.JSX.Element;
